[package]
name = "terminal-test-app"
version = "0.1.0"
edition = "2021"
license = "Apache-2.0"

[[bin]]
name = "terminal-test-app"
path = "src/main.rs"

[dependencies]
terminal = { path = "../terminal" }
terminal_view = { path = "../terminal_view" }
gpui = "0.2.2"
gpui-component = "0.4.0"
